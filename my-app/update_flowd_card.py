import re

file_path = '/Users/bytedance/Documents/trae_projects/Flowd/my-app/src/components/FlowdCard.tsx'

with open(file_path, 'r') as f:
    content = f.read()

new_cases = """
  // Render MEETING card
  if (card.category === 'meeting') {
    return (
      <div 
        className={`card-meeting p-5 mb-3 cursor-default hover:shadow-lg transition-all duration-200 group animate-fade-in relative rounded-[20px] overflow-hidden flex flex-col ${!isModal ? 'max-h-[420px]' : ''}`}
        onClick={() => onClick?.(card)}
        style={{ backgroundColor: config.bgColor, borderColor: config.borderColor, borderWidth: 1 }}
      >
        <div className="flex items-center justify-between mb-3">
          <div className="flex items-center gap-2">
            <span className="text-xl">{config.icon}</span>
            <span className="text-xs font-bold px-2 py-0.5 rounded bg-white/50" style={{ color: config.textColor }}>
 import re

file_path = '/Users/bytedance/Documents/tr <
file_pa   
with open(file_path, 'r') as f:
    content = f.read()

new_cases = """
  // Render MEETING cacar    content = f.read()

new_cases = ""sN
new_cases = """
  //hov  // Render ME t  if (card.category ===ov    return (
      <div 
        className=        <div ss        clh-        onClick={() => onClick?.(card)}
        style={{ backgroundColor: config.bgColor, borderColor: config.borderColor, borderWidth: 1 }}
      >
        <div className="flex items-center justify-between mb-3">
          <div          style={{ backgroundColor: conf c      >
        <div className="flex items-center justify-between mb-3">
          <div className="" }          <div className="flex items-center gap-2">
                         <span className="text-xl">{config.icont-            <span className="text-xs font-bold px-2 py-0." import re